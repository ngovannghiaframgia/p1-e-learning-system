<% provide :title, t("lessons.index.edit_lessons") %>
<% provide :button_text, t("update") %>
<% provide :method_form, :patch %>
<% provide :path_form, admin_lesson_path %>

<div class="container py-3" >
  <div class="row">
    <div class="mx-auto col-sm-6">
      <div class="card">
        <div class="card-header">
          <h4 class="mb-0"><%= t "lessons.index.edit_lessons" %></h4>
        </div>
        <div class="card-body">
          <%= render "form"%>
        </div>
      </div>
    </div>
  </div>
</div>
